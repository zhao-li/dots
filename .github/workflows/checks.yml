name: Checks

on: push

jobs:
  lint-shell:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Lint Application
        run: |
          source custom/plugins/zhaoli/shellcheck.sh
          lint_project

