name: Test Bootstrap

on: push

jobs:
  bootstrap:
    runs-on:
      - macos-latest
      - ubuntu-latest
      - windows-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Bootstrap
        run: ./bootstrap.bash

