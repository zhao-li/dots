name: Test Bootstrap

on: push

jobs:
  strategy:
    matrix:
      os: [ macos-latest, ubuntu-latest, windows-latest ]
  runs-on: ${{ matrix.os }}

  bootstrap:
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Bootstrap
        run: ./bootstrap.bash

